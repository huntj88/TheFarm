CREATE TABLE log
(
    log_id      INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    time        TEXT NOT NULL,
    level       TEXT NOT NULL,
    tag         TEXT NOT NULL,
    description TEXT NOT NULL,
    exception   TEXT
);

insert:
INSERT INTO log(time, level, tag, description, exception)
VALUES (?, ?, ?, ?, ?);


